import test from 'node:test';import assert from 'node:assert/strict';import {EventEmitter} from 'node:events';
process.env.OPENAI_API_KEY='test-key';process.env.APP_PASSWORD='test-password';
const {chat}=await import('../lib/providers.js');const {guard,session}=await import('../lib/access.js');
function response(){const r=new EventEmitter();r.writeHead=(s)=>r.status=s;r.end=()=>{};return r;}
test('access rejects missing session and accepts signed cookie',()=>{let r=response();assert.equal(guard({headers:{}},r),false);assert.equal(r.status,401);assert.equal(guard({headers:{cookie:'converse_session='+session()}},response()),true);assert.equal(guard({headers:{cookie:'converse_session=1.fake'}},response()),false);});
test('origin protection rejects foreign origins',()=>{assert.equal(guard({headers:{host:'localhost',origin:'https://evil.example',cookie:'converse_session='+session()}},response()),false);});
test('stream parser handles split events and preserves speaker ownership',async()=>{const original=globalThis.fetch;let sent;globalThis.fetch=async(url,options)=>{sent=JSON.parse(options.body);const data=['data: {"type":"response.output_text.delta","delta":"Hi"}\r\n\r','\ndata: {"type":"response.output_text.delta","delta":" there"}\n\ndata: {"type":"response.completed"}\n\n'];return new Response(new ReadableStream({start(c){for(const d of data)c.enqueue(new TextEncoder().encode(d));c.close();}}));};try{let text='';await chat({provider:'GPT',model:'gpt-4o-2024-11-20',messages:[{role:'user',content:'Hi'},{role:'assistant',provider:'Claude',model:'claude-sonnet-5',content:'Hello'},{role:'user',content:'Who said hello?'}]},s=>text+=s);assert.equal(text,'Hi there');assert.equal(sent.model,'gpt-4o-2024-11-20');assert.equal(sent.input[1].role,'user');assert.match(sent.input[1].content,/Claude/);}finally{globalThis.fetch=original;}});
test('truncated streams fail instead of claiming success',async()=>{const original=globalThis.fetch;globalThis.fetch=async()=>new Response('data: {"type":"response.output_text.delta","delta":"partial"}\n\n');try{await assert.rejects(()=>chat({provider:'GPT',model:'gpt-4o-2024-11-20',messages:[{role:'user',content:'Hi'}]},()=>{}),/unexpectedly/);}finally{globalThis.fetch=original;}});
